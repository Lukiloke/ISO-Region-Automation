A python script that automatically finds the ISO3166 (or ISO Region Code) from a ZIP Code and a Country code. In the image below is the correct formatting for the input CSV file: first column is country code and second column is ZIP code:

![Alt Text](image.png "Example Image")

To run this script you need the followinf librairies:

```
geopy
pgeocode
sys
csv
time
```

